apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: data-transport-layer-pvc
spec:
  storageClassName: local-path
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
