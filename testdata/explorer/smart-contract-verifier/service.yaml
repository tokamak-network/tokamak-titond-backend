apiVersion: v1
kind: Service
metadata:
  name: smart-contract-verifier-svc
spec:
  selector:
    app: smart-contract-verifier
  ports:
    - name: smart-contract-verifier
      port: 8050
      targetPort: 8050
